<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CCB"
        tools:targetApi="31">
        <activity
            android:name=".controller.MainActivity"
            android:windowSoftInputMode="adjustPan"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".controller.JoinFirstActivity"></activity>
        <activity android:name=".controller.JoinSecondActivity"></activity>
        <activity android:name=".controller.LoginActivity"></activity>
        <activity android:name=".controller.Register_receipe"></activity>
        <activity android:name=".controller.Me"></activity>
        <activity android:name=".controller.Image_select_page"></activity>
        <activity android:name=".controller.SearchListActivity"></activity>
        <activity android:name=".controller.RecipeDocActivity"/>
        <activity android:name=".controller.Search_home"/>


    </application>
    
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>